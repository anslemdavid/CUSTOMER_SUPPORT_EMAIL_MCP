{
  "name": "customer-support-email-mcp",
  "version": "1.0.0",
  "description": "MCP for Claude to read, search, draft, and send customer support emails via Gmail",
  "main": "dist/gmail_tool.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/gmail_tool.js",
    "dev": "ts-node src/gmail_tool.ts",
    "test": "npm run build && npm start"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.20.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  }
}